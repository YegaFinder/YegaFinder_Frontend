# Base URL of the backend REST API.
# NEXT_PUBLIC_ prefix is required so this is readable in client components.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Google Maps API key (needed starting Sprint 3 — business listings map view)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
