# profile (Sprint 2)

Planned structure, mirroring `features/auth/`:

```
profile/
├── api/profile.api.ts
├── types/profile.types.ts
├── schemas/
├── hooks/
└── components/
```

Covers: customer profile (personal info, photo, saved places, favorites)
and merchant profile (business info, logo/banner, hours, contact) per
the SRS section 3.2.
